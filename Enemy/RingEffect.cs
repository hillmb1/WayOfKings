using UnityEngine;
using System.Collections;

public class RingEffect : MonoBehaviour {

	public string ringtype;

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
		if (ringtype != null) {
			if (ringtype == "Fire ring") {
				
			}
		}
	}

}
